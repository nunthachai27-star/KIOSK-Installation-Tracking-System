services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: kiosk
      POSTGRES_PASSWORD: kiosk
      POSTGRES_DB: kiosk
    ports:
      - "5434:5432"
    volumes:
      - kiosk_pgdata:/var/lib/postgresql/data
volumes:
  kiosk_pgdata:
