import { BrandLoading } from '@/components/BrandLoading'

// โหลดดิ้งของหน้าเจ้าหน้าที่ทั้งหมด (แสดงในพื้นที่เนื้อหา โดยแถบหัวเว็บยังอยู่)
export default function Loading() {
  return <BrandLoading />
}
